<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zy.dao.RoomMapper">
    <select id="selectRoomByType" parameterType="Integer" resultType="zy.pojo.room">
    select * from room_info where room_type = #{type}
    </select>
</mapper>
    